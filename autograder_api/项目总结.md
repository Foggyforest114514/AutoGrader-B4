# AutoGrader API 项目生成总结

## 📁 项目结构

```
autograder_api/
├── app/
│   ├── models/
│   │   ├── __init__.py
│   │   └── models.py              # 数据库模型（7个表）
│   ├── routers/
│   │   ├── __init__.py
│   │   ├── auth.py                # 认证接口（登录、登出、刷新Token）
│   │   ├── users.py               # 用户管理接口
│   │   ├── courses.py             # 课程管理接口
│   │   ├── classes.py             # 班级管理接口
│   │   ├── assignments.py         # 作业管理接口
│   │   ├── submissions.py         # 提交管理接口
│   │   └── grades.py              # 成绩管理接口
│   ├── __init__.py
│   ├── main.py                    # FastAPI主应用
│   ├── database.py                # 数据库配置
│   ├── schemas.py                 # Pydantic数据验证模型
│   └── auth.py                    # JWT认证模块
├── requirements.txt               # Python依赖包
├── .env.example                  # 环境变量配置示例
├── README.md                     # 项目说明文档
├── start.bat                     # Windows启动脚本
└── start.sh                      # Linux/Mac启动脚本
```

## 🗄️ 数据库表结构

根据 `dump-autograder-202604131426.sql` 生成的数据库模型：

1. **users** - 用户表
   - 支持学生、教师、管理员三种角色
   - 包含用户名、密码、邮箱、真实姓名等字段

2. **students** - 学生扩展表
   - 与users表一对一关系
   - 包含学号、首次密码修改标记

3. **teachers** - 教师扩展表
   - 与users表一对一关系
   - 包含工号、院系

4. **courses** - 课程表
   - 课程编号、课程名称、学期
   - 与教师多对一关系

5. **classes** - 班级表
   - 班级名称、课序号
   - 与课程多对一关系

6. **class_students** - 学生-班级关联表
   - 多对多关系表
   - 记录学生加入班级的时间

7. **assignments** - 作业表
   - 作业标题、描述、截止时间
   - 与班级、教师、题目关联

8. **submissions** - 提交表
   - 学生提交的代码
   - 评测状态、得分、测试用例结果（JSON格式）

## 🔌 API接口列表

### 认证模块 (Auth)
- `POST /api/v1/auth/login` - 用户登录
- `POST /api/v1/auth/logout` - 用户登出
- `POST /api/v1/auth/refresh` - 刷新Token
- `POST /api/v1/auth/reset-password` - 重置密码

### 用户模块 (Users)
- `GET /api/v1/users/me` - 获取当前用户信息
- `PUT /api/v1/users/me` - 修改当前用户信息
- `GET /api/v1/users` - 管理员获取用户列表
- `POST /api/v1/users` - 管理员创建教师账号
- `POST /api/v1/users/{id}/deactivate` - 停用账号

### 课程模块 (Courses)
- `GET /api/v1/courses` - 获取课程列表
- `POST /api/v1/courses` - 创建课程
- `GET /api/v1/courses/{id}` - 获取课程详情
- `PUT /api/v1/courses/{id}` - 修改课程
- `DELETE /api/v1/courses/{id}` - 删除课程

### 班级模块 (Classes)
- `GET /api/v1/classes` - 获取班级列表
- `POST /api/v1/classes` - 创建班级
- `GET /api/v1/classes/{id}/students` - 查看班级学生
- `POST /api/v1/classes/{id}/students` - 手动添加学生
- `POST /api/v1/classes/{id}/students/import` - 批量导入学生
- `DELETE /api/v1/classes/{id}/students/{sid}` - 移除学生

### 作业模块 (Assignments)
- `GET /api/v1/assignments` - 获取作业列表
- `POST /api/v1/assignments` - 创建作业
- `GET /api/v1/assignments/{id}` - 获取作业详情
- `PUT /api/v1/assignments/{id}` - 修改作业
- `POST /api/v1/assignments/{id}/publish` - 发布作业

### 提交模块 (Submissions)
- `POST /api/v1/submissions` - 提交作业
- `GET /api/v1/submissions/{id}` - 获取提交详情
- `PATCH /api/v1/submissions/{id}/result` - 更新评测结果（B-2模块调用）
- `GET /api/v1/submissions/my` - 获取我的提交记录

### 成绩模块 (Grades)
- `GET /api/v1/grades/my` - 学生查询自己成绩
- `GET /api/v1/grades/class/{class_id}` - 教师查询班级成绩
- `GET /api/v1/grades/export/{assignment_id}` - 导出作业成绩表

## 🔐 认证与权限

### JWT Token认证
- 使用JWT进行用户认证
- Access Token有效期：24小时
- Refresh Token有效期：7天
- 支持Token刷新机制

### 权限级别
- **student**: 只能访问自己的课程、作业、提交、成绩
- **teacher**: 只能管理自己的课程、班级、作业、学生
- **admin**: 全平台管理权限

## 🚀 快速启动

### 1. 安装依赖
```bash
pip install -r requirements.txt
```

### 2. 配置环境变量
```bash
cp .env.example .env
# 编辑.env文件，配置数据库连接信息
```

### 3. 运行服务

**Windows:**
```bash
start.bat
```

**Linux/Mac:**
```bash
chmod +x start.sh
./start.sh
```

**或直接运行:**
```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 4. 访问API文档
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

## 📝 特色功能

### 1. 自动API文档
- FastAPI自动生成Swagger UI文档
- 支持在线测试API接口
- 详细的请求/响应示例

### 2. 数据验证
- 使用Pydantic进行数据验证
- 自动类型转换和校验
- 友好的错误提示

### 3. 异常处理
- 全局异常捕获
- 统一的错误响应格式
- 详细的日志记录

### 4. 数据库优化
- 连接池配置（适配512MB内存限制）
- 外键级联删除
- 索引优化

### 5. 安全性
- 密码bcrypt加密
- JWT Token认证
- CORS跨域配置
- SQL注入防护

## 🔧 配置说明

### 数据库配置
```env
DATABASE_URL=mysql+pymysql://username:password@host:port/database_name
```

### 安全配置
```env
SECRET_KEY=your-secret-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_HOURS=24
REFRESH_TOKEN_EXPIRE_DAYS=7
```

### Redis配置（可选）
```env
REDIS_URL=redis://localhost:6379/0
```

## 📊 符合需求规格

根据《数据库模块间API调用接口规范 v2.1》文档：

✅ RESTful API设计风格
✅ 统一返回格式（code、msg、data）
✅ JWT Token认证
✅ 权限控制（student、teacher、admin）
✅ 支持B-2模块回写评测结果
✅ 支持文件上传（学生批量导入）
✅ 支持成绩导出

## 🎯 下一步工作

1. **测试**: 编写单元测试和集成测试
2. **部署**: 配置生产环境部署脚本
3. **监控**: 添加日志监控和性能监控
4. **优化**: 根据实际使用情况优化查询性能
5. **文档**: 完善API使用示例和最佳实践

## 📞 技术支持

如有问题，请查看：
1. README.md 文档
2. API文档: http://localhost:8000/docs
3. 日志输出

---

**生成时间**: 2026-04-13
**版本**: 1.0.0
**框架**: FastAPI
**数据库**: MySQL
